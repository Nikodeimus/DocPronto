{
  "name": "docpronto",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check server.mjs && node --check server-cert-v8.mjs && node --check hub/app.js",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run check && npm test"
  }
}
